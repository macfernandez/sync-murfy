[project]
name = "sync-murfy"
version = "0.1.0"
description = "GitHub Action to sync a Murfy LaTeX project with a git repository"
requires-python = ">=3.11"
dependencies = [
    "selenium>=4.0",
    "webdriver-manager>=4.0",
    "python-dotenv>=1.0",
]
